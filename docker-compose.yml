services:
  bot:
    build: .
    init: true
    ipc: host
    env_file: .env
    restart: unless-stopped
    volumes:
      - bot-data:/app/data
    environment:
      - DB_PATH=/app/data/jobs.db

volumes:
  bot-data:
